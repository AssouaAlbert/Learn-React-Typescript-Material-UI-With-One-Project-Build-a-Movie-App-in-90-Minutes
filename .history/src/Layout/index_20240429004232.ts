import { ReactNode } from "react";

interface LayoutProps {
  childrend: ReactNode;
}
import React from 'react'

function Layout({childrend} : layout) {
  return (
    <div>
      
    </div>
  )
}

export default Layout

export default LayoutProps;
